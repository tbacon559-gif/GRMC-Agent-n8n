# Prompt Library

**Not yet populated.** No AI Prompt Library shared service exists yet —
see `docs/architecture/05-ai-agent-inventory.md` for the current
scattered state (prompts live inline in `src/core/ai/*` and
`src/modules/marketplace/ai/*`, and copy-pasted across n8n workflow
nodes, most notably the ~5x-duplicated Church "crisis-screen" prompt).

This file becomes the canonical prompt catalog once the Prompt Library
shared service is built (`docs/architecture/10-implementation-phases.md`
Phase 2 for the first extraction, Phase 6 for the formal service).
